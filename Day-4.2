public static void Sort( int arr[])
{
    int low =0 , mid =0 , high= arr.length-1;
    while(mid<=high)
    {
        if(arr[i]==0)
        {
            swap(low,mid,arr); 
            mid++;
            low++;
        }
        else if(arr[i]==2)
        {
            swap(mid,high,arr); 
            high--;
        }
        else 
        {
            mid++;
        }
    }
} 


publc static void swap (int x , int y , int arr)
{
    int temp = arr[x];
    arr[x] = arr[y];
    arr[y] = temp ;
}